sudo: required
services:
  - docker

# Runs before build
before_install:
  - docker build -t hammadjafar/docker -f Dockerfile.dev .

script: 
  - docker run -e Cl=true hammadjafar/docker npm run test -- --coverage
